{
  "name": "cardano-signer",
  "version": "1.2.0",
  "description": "cardano-signer signs a given data(hex) with a signing key(file or hex) or verify the signature via a public key(file or hex)",
  "main": "cardano-signer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@gitmachtl - ATADA Stakepools Austria",
  "license": "MIT",
  "dependencies": {
    "@emurgo/cardano-serialization-lib-nodejs": "^11.0.5"
  }
}
